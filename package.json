{
  "name": "mcp-md-tasks",
  "version": "1.0.0",
  "description": "MCP server for managing task cards in local markdown files",
  "type": "module",
  "bin": {
    "mcp-md-tasks": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.25.76"
  }
}
